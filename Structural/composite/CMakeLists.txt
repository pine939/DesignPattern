CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT("composite")
set (CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11")
SET(OUTPUT_FILE composite)
SET(SRC_FILE
        Composite.cpp)

ADD_EXECUTABLE(${OUTPUT_FILE} ${SRC_FILE})
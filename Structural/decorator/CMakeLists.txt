CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT("decorator")
set (CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11")
SET(OUTPUT_FILE decorator)
SET(SRC_FILE
        Decorator.cpp)

ADD_EXECUTABLE(${OUTPUT_FILE} ${SRC_FILE})